// Declare the message we want to broadcast
let message = 'Hello, world!';

// Log any message we provide to the function
const sayHello = message => {
    console.log(message);
}

// Execute our sayHello function to log our message
sayHello(message);